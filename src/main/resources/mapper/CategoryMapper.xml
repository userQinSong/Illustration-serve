<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="alibaba.illustration.dao.ill.CategoryDao">
    <select id="queryAllTypes" resultType="java.lang.String">
        select type_name from cat_type where is_delete = 0
    </select>
    <select id="queryAllTags" resultType="java.lang.String">
        select tag_name from cat_tag where is_delete = 0
    </select>
</mapper>